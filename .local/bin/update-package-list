#!/bin/sh
#
# This script creates three files "packages-pacman.txt", and "packages-foreign"
#
#	packages-pacman.txt:	contains all user installed applications.
#	packages-foreign.txt:	contains all packages that were not installed via
#							pacman, but via an alternate package manager. i.e yay.
#	packages.txt:			contains all packages currently installed on the system.

pacman -Qett | grep -v "$(pacman -Qem)" > packages-pacman.txt
pacman -Qqm > packages-foreign.txt
pacman -Q > packages.txt
